<?xml version="1.0" encoding="UTF-8"?>
<!--
BackTestSys 回测系统配置文件
所有可调整参数的配置文档
-->
<config>
    <!-- ============================================================================
         数据配置 (Data Configuration)
         ============================================================================ -->
    <data>
        <!-- 市场数据文件路径，支持pickle格式和csv格式的数据文件 -->
        <path>data/sample.csv</path>
        
        <!-- 数据格式：pkl (pickle) 或 csv -->
        <format>csv</format>
    </data>

    <!-- ============================================================================
         Tape构建器配置 (Tape Builder Configuration)
         ============================================================================ -->
    <tape>
        <!-- 时间分配的基准权重 (无成交时的baseline)
             用于防止零长度段的产生，典型值: 1.0 -->
        <epsilon>1.0</epsilon>
        
        
        <!-- 时间缩放lambda参数
             用于控制事件早期/晚期的分布
             0.0 = 均匀分布 -->
        <time_scale_lambda>0.0</time_scale_lambda>
        
        
        <!-- 跟踪的价格档位数 (Top-K)，典型值: 5 (跟踪前5档) -->
        <top_k>5</top_k>
        
        <!-- tick_size: 最小价格变动单位 -->
        <tick_size>1.0</tick_size>
    </tape>

    <!-- ============================================================================
         交易所模拟器配置 (Exchange Simulator Configuration)
         ============================================================================ -->
    <exchange>
        <!-- 撤单偏置参数 k
             取值范围: -1.0 ~ 1.0
             - k < 0: 撤单更偏向队列后部
             - k = 0: 近似均匀
             - k > 0: 撤单更偏向队列前部 -->
        <cancel_bias_k>0.0</cancel_bias_k>
    </exchange>

    <!-- ============================================================================
         运行器配置 (Runner Configuration)
         ============================================================================ -->
    <runner>
        <!-- 策略 -> 交易所 的延迟 (订单发送延迟)
             单位: tick (每tick = 100ns)
             例如: 1000000 = 100ms -->
        <delay_out>0</delay_out>
        
        <!-- 交易所 -> 策略 的延迟 (回执接收延迟)
             单位: tick (每tick = 100ns) -->
        <delay_in>0</delay_in>
        
        <!-- 是否显示进度条 (需要安装tqdm) -->
        <show_progress>false</show_progress>
    </runner>

    <!-- ============================================================================
         投资组合配置 (Portfolio Configuration)
         ============================================================================ -->
    <portfolio>
        <!-- 初始现金 -->
        <initial_cash>100000.0</initial_cash>
    </portfolio>

    <!-- ============================================================================
         策略配置 (Strategy Configuration)
         ============================================================================ -->
    <strategy>
        <!-- 策略名称: SimpleStrategyImpl | ReplayStrategy / ReplayStrategy_Impl -->
        <name>ReplayStrategy_Impl</name>
        
        <!-- 策略特定参数 (可根据策略自定义) -->
        <params>
            <!-- SimpleStrategy: 每隔多少个快照下单 -->
            <!-- <order_interval>10</order_interval> -->
            <!-- SimpleStrategy: 最大活跃订单数 -->
            <!-- <max_active_orders>5</max_active_orders> -->
            <!-- ReplayStrategy: 下单CSV文件路径 -->
            <order_file></order_file>
            <!-- ReplayStrategy: 撤单CSV文件路径 -->
            <cancel_file></cancel_file>
        </params>
    </strategy>

    <!-- ============================================================================
         回执记录器配置 (Receipt Logger Configuration)
         ============================================================================ -->
    <receipt_logger>
        <!-- 是否实时打印回执 -->
        <verbose>false</verbose>
        
        <!-- 回执保存路径 (可选，留空则不保存)
             可以是完整文件路径 (如 output/receipts.csv)
             也可以是文件夹路径 (如 output/)，程序会自动生成带时间戳的CSV文件名 -->
        <output_file></output_file>
    </receipt_logger>

    <!-- ============================================================================
         回测结果落盘配置 (Run Result Output Configuration)
         ============================================================================ -->
    <run_result>
        <!-- 回测结果落盘路径 (可选，留空则不保存)
             可以是目录 (如 output/)，程序会创建 run_result_合约_时间戳 子目录并写入 4 个 CSV 及 contract_info.csv
             可以是文件基础路径 (如 output/result)，程序会生成 result_合约_DoneInfo.csv 等 4 个文件及 contract_info.csv
             合约信息来自 contract，同时会落盘到 contract_info.csv -->
        <output_file></output_file>
    </run_result>

    <!-- ============================================================================
         可观测流配置 (Observability Stream Configuration)
         ============================================================================ -->
    <observability_stream>
        <!-- 运行中历史事件的落盘目录 -->
        <history_dir>.obs_history</history_dir>

        <!-- 单个订阅者缓冲区内存上限（MB） -->
        <subscriber_max_memory_mb>8</subscriber_max_memory_mb>
    </observability_stream>

    <!-- ============================================================================
         日志配置 (Logging Configuration)
         ============================================================================ -->
    <logging>
        <!-- 是否启用debug模式 -->
        <debug>false</debug>
        
        <!-- 日志保存路径 (可选，留空则不保存到文件)
             可以是完整文件路径 (如 output/backtest.log)
             也可以是文件夹路径 (如 output/)，程序会自动生成带时间戳的日志文件名 -->
        <log_file></log_file>
        
        <!-- 日志级别 (DEBUG, INFO, WARNING, ERROR) -->
        <level>INFO</level>
        
        <!-- 是否在终端打印日志 -->
        <console>true</console>
    </logging>

    <!-- ============================================================================
         快照处理配置 (Snapshot Processing Configuration)
         ============================================================================ -->
    <snapshot>
        <!-- 快照最小间隔 (tick单位)
             500ms = 5,000,000 ticks (每tick = 100ns) -->
        <min_interval_tick>5000000</min_interval_tick>
        
        <!-- 时间容差 (tick单位)
             用于处理RecvTick的时间抖动
             10ms = 100,000 ticks -->
        <tolerance_tick>100000</tolerance_tick>
    </snapshot>

    <!-- ============================================================================
         合约配置 (Contract Configuration)
         ============================================================================ -->
    <contract>
        <!-- 合约ID (用于从合约字典中查找对应合约信息) -->
        <contract_id></contract_id>
        
        <!-- 合约字典XML文件路径 (包含所有合约的详细信息)
             XML格式示例：
             <ContractDictionaryConfig>
                 <Contract>
                     <ContractId>IF2401</ContractId>
                     <TickSize>0.2</TickSize>
                     <ExchangeCode>CFFEX</ExchangeCode>
                     <TradingHours>
                         <TradingHour>
                             <StartTime>09:30:00</StartTime>
                             <EndTime>11:30:00</EndTime>
                         </TradingHour>
                         <TradingHour>
                             <StartTime>13:00:00</StartTime>
                             <EndTime>15:00:00</EndTime>
                         </TradingHour>
                     </TradingHours>
                 </Contract>
             </ContractDictionaryConfig>
        -->
        <contract_dictionary_path></contract_dictionary_path>
    </contract>
</config>
